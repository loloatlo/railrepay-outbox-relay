{
  "default": {
    "host": "${PGHOST}",
    "port": "${PGPORT}",
    "database": "${PGDATABASE}",
    "user": "${PGUSER}",
    "password": "${PGPASSWORD}",
    "schema": "outbox_relay"
  }
}
